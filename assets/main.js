(() => {
  const MR_BY_EN = {
    Home: "मुख्यपृष्ठ",
    "About Us": "आमच्याबद्दल",
    News: "बातम्या",
    Contact: "संपर्क",
    "Enquire Now": "तुरंत चौकशी करा",
    "Skip to content": "मुख्य मजकुराकडे जा",
    "Manikrao Patil Education Foundation": "कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन",
    "Shaping futures through education": "शिक्षणातून भविष्य घडवतो",
    "Education â€¢ Excellence â€¢ Empowerment": "शिक्षण • उत्कृष्टता • सबलीकरण",
    "Mission â€¢ Vision â€¢ Values": "ध्येय • दृष्टिकोन • मूल्ये",
    "Letâ€™s build something meaningful together": "चला, एकत्र येऊन काहीतरी अर्थपूर्ण घडवूया",
    "For admissions, partnerships, or support, reach out and weâ€™ll respond quickly.":
      "प्रवेश, भागीदारी किंवा सहकार्यासाठी आमच्याशी संपर्क साधा; आम्ही लवकरच प्रतिसाद देऊ.",
    "Phone, email, and address are placeholdersâ€”replace them with your real details.":
      "फोन, ईमेल आणि पत्ता हे नमुना स्वरूपात दिले आहेत—कृपया तुमची खरी माहिती वापरा.",
    "Share your details and weâ€™ll get back to you with next steps.":
      "तुमची माहिती शेअर करा; पुढील प्रक्रियेबद्दल आम्ही लवकरच संपर्क करू.",
    "Trusted education initiative • Community-first • Student success":
      "विश्वसनीय शैक्षणिक उपक्रम • समुदाय-केंद्रित • विद्यार्थी यश",
    "Building brighter futures with <span class='text-gradient'>quality education</span>":
      "गुणवत्तापूर्ण शिक्षणातून <span class='text-gradient'>उज्ज्वल भविष्य</span> घडवणे",
    "Explore About Us": "आमच्याबद्दल जाणून घ्या",
    "See Our News": "आमच्या बातम्या पहा",
    "What we focus on": "आमचे मुख्य केंद्रबिंदू",
    "A foundation you can trust": "विश्वासार्ह शैक्षणिक संस्था",
    "Read our story": "आमची कथा वाचा",
    "Want admissions / collaboration details?": "प्रवेश किंवा सहकार्याची माहिती हवी आहे?",
    "Contact MPEF": "MPEF शी संपर्क करा",
    Fast: "त्वरित",
    Response: "प्रत्युत्तर",
    Clear: "स्पष्ट",
    Guidance: "मार्गदर्शन",
    Support: "सहाय्य",
    Always: "नेहमी",
    "Get in touch": "संपर्क साधा",
    Phone: "फोन",
    Email: "ईमेल",
    Address: "पत्ता",
    "Office Hours": "कार्यालयीन वेळा",
    "Mon - Sat: 10:00 AM - 6:00 PM": "सोम - शनि: सकाळी 10:00 ते सायं 6:00",
    Enquiry: "चौकशी",
    Name: "नाव",
    Message: "संदेश",
    "Send enquiry (demo)": "चौकशी पाठवा (डेमो)",
    "Quick Links": "त्वरित दुवे",
    "Our Work": "आमचे कार्य",
    Gallery: "गॅलरी",
    "Contact Us": "आमच्याशी संपर्क",
    "Privacy Policy": "गोपनीयता धोरण",
    "Terms of Service": "सेवा अटी",
    "Follow Us": "आम्हाला फॉलो करा",
    "Stay connected with our latest updates and events.":
      "आमच्या ताज्या अपडेट्स आणि कार्यक्रमांसोबत जोडलेले रहा.",
    "Late Manikrao Patil Educational Foundation": "कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन",
    "Registered under RG. No. F33881": "नोंदणी क्रमांक: RG. No. F33881",
    "© 2026 Late Manikrao Patil Educational Foundation. All rights reserved.":
      "© 2026 कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन. सर्व हक्क राखीव.",
    "About MPEF": "MPEF विषयी",
    "Our mission": "आमचे ध्येय",
    "Our vision": "आमचा दृष्टिकोन",
    "Our values": "आमची मूल्ये",
    "What we offer": "आमच्या सुविधा",
    "View News": "बातम्या पहा",
    "Join Our Mission to Empower Teachers": "शिक्षक सक्षमीकरणाच्या आमच्या ध्येयात सहभागी व्हा",
    "News & Media": "बातम्या व माध्यमे",
    Clippings: "वृत्तपत्र कात्रणे",
    "MPEF Gallery": "MPEF गॅलरी",
    "Photo Gallery": "छायाचित्र गॅलरी",
    "Campus and students": "परिसर आणि विद्यार्थी",
    "Learning and activities": "अभ्यास आणि उपक्रम",
    "Events and community": "कार्यक्रम आणि समुदाय",
    "Events and celebrations": "कार्यक्रम आणि साजरेक्षण",
    "Newspaper clipping 1": "वृत्तपत्र कात्रण 1",
    "Newspaper clipping 2": "वृत्तपत्र कात्रण 2",
    "Newspaper clipping 3": "वृत्तपत्र कात्रण 3",
    "Newspaper clipping 4": "वृत्तपत्र कात्रण 4",
    "MPEF emblem": "MPEF चिन्ह",
    "Campus • Activities • News": "परिसर • उपक्रम • बातम्या",
    Campus: "परिसर",
    Life: "जीवन",
    Events: "कार्यक्रम",
    Snapshots: "झलक",
    Media: "माध्यम",
    Coverage: "प्रसिद्धी",
    "Moments that matter": "महत्त्वाचे क्षण",
    "Kolhapur, Maharashtra, India": "कोल्हापूर, महाराष्ट्र, भारत",
    "Quality": "गुणवत्ता",
    Teaching: "अध्यापन",
    Discipline: "शिस्त",
    "mindset": "दृष्टिकोन",
    "& care": "आणि काळजी",
    Growth: "विकास",
    "Images are loaded from <code>images/Newspaper/</code>.":
      "येथील सर्व छायाचित्रे <code>images/Newspaper/</code> मधून घेतली जातात.",
    "Below are recent clippings. Add more images to <code>images/Newspaper/</code> and we can include them here as well.":
      "खाली अलीकडील बातम्यांच्या कात्रणांचा संग्रह दिला आहे. <code>images/Newspaper/</code> मध्ये नवीन फोटो जोडल्यास ते येथेही दाखवता येतील.",
  };

  const MR_PLACEHOLDER_BY_EN = {
    "Your full name": "तुमचे पूर्ण नाव",
    "How can we help you?": "आम्ही तुम्हाला कशी मदत करू?",
    "you@example.com": "तुमचा ईमेल आयडी",
  };

  const MR_ARIA_BY_EN = {
    "Open menu": "मेनू उघडा",
    "Close": "बंद करा",
    "Enquiry form": "चौकशी फॉर्म",
  };
  const domMrByEn = {};
  const domMrPlaceholderByEn = {};
  const domMrAriaByEn = {};
  const autoTextNodes = [];
  const autoTextOriginal = new WeakMap();
  const header = document.querySelector(".site-header");
  if (header) {
    const onScroll = () => {
      if (window.scrollY > 18) header.classList.add("is-scrolled");
      else header.classList.remove("is-scrolled");
    };
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
  }

  const yearEl = document.getElementById("year");
  if (yearEl) yearEl.textContent = String(new Date().getFullYear());

  const nav = document.querySelector("[data-nav]");
  const toggle = document.querySelector("[data-nav-toggle]");
  if (nav && toggle) {
    toggle.addEventListener("click", () => {
      nav.classList.toggle("is-open");
    });

    document.addEventListener("click", (e) => {
      const t = e.target;
      if (!(t instanceof Element)) return;
      const clickedInside = nav.contains(t) || toggle.contains(t);
      if (!clickedInside) nav.classList.remove("is-open");
    });
  }

  // Enquiry popup after successful form validation
  const enquiryForm = document.querySelector("form.form");
  if (enquiryForm) {
    enquiryForm.addEventListener("submit", (e) => {
      e.preventDefault();
      if (!enquiryForm.reportValidity()) return;

      const isMr = document.documentElement.getAttribute("data-lang") === "mr";
      const title = isMr ? "धन्यवाद!" : "Thank you!";
      const msg = isMr
        ? "संपर्क केल्याबद्दल धन्यवाद. आम्ही शक्य तितक्या लवकर तुमच्याशी संपर्क करू."
        : "Thank you for contacting us. We will get back to you as soon as possible.";
      openEnquiryPopup(title, msg, isMr ? "ठीक आहे" : "OK");
      enquiryForm.reset();
    });
  }

  // News page grid
  const newsGrid = document.getElementById("newsGrid");
  const images = Array.isArray(window.MPEF_NEWS_IMAGES) ? window.MPEF_NEWS_IMAGES : null;
  if (newsGrid && images) {
    newsGrid.innerHTML = images
      .map((item, idx) => {
        const title = item.title || `News clipping`;
        const src = item.src;
        return `
          <figure class="news-item" data-open-lightbox data-src="${src}" data-title="${escapeHtml(
          title
        )}">
            <img src="${src}" alt="News clipping" loading="lazy" />
          </figure>
        `;
      })
      .join("");
  }

  // Language toggle
  const storedLang = localStorage.getItem("mpef_lang");
  const initialLang = storedLang === "mr" ? "mr" : "en";
  buildDomTranslationMaps();
  collectAutoTextNodes();
  applyLanguage(initialLang);

  document.querySelectorAll("[data-lang-toggle]").forEach((wrap) => {
    const buttons = wrap.querySelectorAll("[data-lang-btn]");
    buttons.forEach((btn) => {
      btn.addEventListener("click", () => {
        const lang = btn.getAttribute("data-lang-btn");
        if (!lang) return;
        applyLanguage(lang);
      });
    });
  });

  // Hero slider on home page
  const heroSlides = document.querySelectorAll("[data-hero-slide]");
  const heroDotsContainer = document.querySelector("[data-hero-dots]");
  if (heroSlides.length > 0 && heroDotsContainer) {
    let current = 0;
    const total = heroSlides.length;

    const dots = Array.from({ length: total }).map((_, idx) => {
      const dot = document.createElement("button");
      dot.type = "button";
      dot.className = "hero-dot" + (idx === 0 ? " is-active" : "");
      dot.setAttribute("aria-label", `Go to slide ${idx + 1}`);
      dot.addEventListener("click", () => {
        goTo(idx);
        restart();
      });
      heroDotsContainer.appendChild(dot);
      return dot;
    });

    function goTo(next) {
      heroSlides[current].classList.remove("is-active");
      dots[current].classList.remove("is-active");
      current = next;
      heroSlides[current].classList.add("is-active");
      dots[current].classList.add("is-active");
    }

    let timer = setInterval(() => {
      const next = (current + 1) % total;
      goTo(next);
    }, 4500);

    function restart() {
      clearInterval(timer);
      timer = setInterval(() => {
        const next = (current + 1) % total;
        goTo(next);
      }, 4500);
    }
  }

  // Scroll reveal animations
  const revealTargets = document.querySelectorAll(
    ".hero-copy, .hero-media, .section-head, .card, .stat, .callout, .gallery-item, .news-item, .mosaic-item, .cta, .page-hero-inner > *"
  );
  revealTargets.forEach((el, idx) => {
    el.classList.add("reveal");
    el.style.transitionDelay = `${Math.min(idx * 45, 320)}ms`;
  });
  const revealObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          revealObserver.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.14, rootMargin: "0px 0px -8% 0px" }
  );
  revealTargets.forEach((el) => revealObserver.observe(el));

  // Subtle mouse-parallax for hero image area
  const heroMedia = document.querySelector(".hero-media");
  if (heroMedia) {
    heroMedia.addEventListener("mousemove", (e) => {
      const r = heroMedia.getBoundingClientRect();
      const x = ((e.clientX - r.left) / r.width - 0.5) * 8;
      const y = ((e.clientY - r.top) / r.height - 0.5) * 8;
      heroMedia.style.setProperty("--parallax-x", `${x}px`);
      heroMedia.style.setProperty("--parallax-y", `${y}px`);
    });
    heroMedia.addEventListener("mouseleave", () => {
      heroMedia.style.setProperty("--parallax-x", "0px");
      heroMedia.style.setProperty("--parallax-y", "0px");
    });
  }

  function applyLanguage(lang) {
    const safeLang = lang === "mr" ? "mr" : "en";
    document.documentElement.setAttribute("data-lang", safeLang);
    localStorage.setItem("mpef_lang", safeLang);
    applyDocumentTitle(safeLang);

    document.querySelectorAll("[data-lang-toggle]").forEach((wrap) => {
      wrap.querySelectorAll("[data-lang-btn]").forEach((btn) => {
        const value = btn.getAttribute("data-lang-btn");
        if (value === safeLang) btn.classList.add("is-active");
        else btn.classList.remove("is-active");
      });
    });

    document.querySelectorAll("[data-i18n]").forEach((el) => {
      const attr = safeLang === "mr" ? "data-mr" : "data-en";
      const raw = el.getAttribute(attr);
      const enKey = normalizeI18n(el.getAttribute("data-en") || "");
      const val =
        safeLang === "mr"
          ? MR_BY_EN[enKey] || domMrByEn[enKey] || normalizeI18n(raw)
          : normalizeI18n(raw);
      if (val) {
        // Keep decorative dot in pills while replacing only the label text.
        if (el.classList.contains("pill")) {
          const dot = el.querySelector(".dot");
          if (dot) {
            Array.from(el.childNodes).forEach((node) => {
              if (node !== dot) node.remove();
            });
            el.appendChild(document.createTextNode(` ${val}`));
            return;
          }
        }
        if (el.hasAttribute("data-i18n-html")) {
          el.innerHTML = val;
          return;
        }
        el.textContent = val;
      }
    });

    document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
      const attr = safeLang === "mr" ? "data-mr-placeholder" : "data-en-placeholder";
      const raw = el.getAttribute(attr);
      const enKey = normalizeI18n(el.getAttribute("data-en-placeholder") || "");
      const val =
        safeLang === "mr"
          ? MR_PLACEHOLDER_BY_EN[enKey] || domMrPlaceholderByEn[enKey] || normalizeI18n(raw)
          : normalizeI18n(raw);
      if (val && "placeholder" in el) {
        el.placeholder = val;
      }
    });

    document.querySelectorAll("[data-i18n-value]").forEach((el) => {
      const attr = safeLang === "mr" ? "data-mr-value" : "data-en-value";
      const raw = el.getAttribute(attr);
      const val = normalizeI18n(raw);
      if (val && "value" in el) {
        el.value = val;
      }
    });

    document.querySelectorAll("[data-i18n-aria-label]").forEach((el) => {
      const attr = safeLang === "mr" ? "data-mr-aria-label" : "data-en-aria-label";
      const raw = el.getAttribute(attr);
      const enKey = normalizeI18n(el.getAttribute("data-en-aria-label") || "");
      const val =
        safeLang === "mr"
          ? MR_ARIA_BY_EN[enKey] || domMrAriaByEn[enKey] || normalizeI18n(raw)
          : normalizeI18n(raw);
      if (val) {
        el.setAttribute("aria-label", val);
      }
    });

    applyAutoTextTranslations(safeLang);
  }

  function normalizeI18n(value) {
    if (!value) return value;
    // Repair mojibake like "à¤®à¤°à¤¾à¤ à¥€" -> "मराठी".
    // Some strings are multi-pass encoded, so decode repeatedly until stable.
    let out = String(value);
    const looksBroken = (s) => /[ÃÂàâ€¢€™¤¥]/.test(s);
    for (let i = 0; i < 4; i += 1) {
      if (!looksBroken(out)) break;
      try {
        const next = new TextDecoder("utf-8").decode(
          Uint8Array.from(out, (ch) => ch.charCodeAt(0) & 255)
        );
        if (!next || next === out) break;
        out = next;
      } catch {
        break;
      }
    }
    return out;
  }

  function applyDocumentTitle(lang) {
    const path = (location.pathname || "").toLowerCase();
    const isAbout = path.endsWith("/about.html") || path.endsWith("\\about.html");
    const isNews = path.endsWith("/news.html") || path.endsWith("\\news.html");
    const isGallery = path.endsWith("/gallery.html") || path.endsWith("\\gallery.html");
    const isPrivacy = path.endsWith("/privacy.html") || path.endsWith("\\privacy.html");
    const isTerms = path.endsWith("/terms.html") || path.endsWith("\\terms.html");
    if (lang === "mr") {
      if (isAbout) {
        document.title = "आमच्याबद्दल • कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      } else if (isNews) {
        document.title = "बातम्या • कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      } else if (isGallery) {
        document.title = "गॅलरी • कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      } else if (isPrivacy) {
        document.title = "गोपनीयता धोरण • कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      } else if (isTerms) {
        document.title = "सेवा अटी • कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      } else {
        document.title = "कै.श्री. माणिकराव पाटील एज्युकेशनल फौंडेशन (MPEF)";
      }
      return;
    }
    if (isAbout) {
      document.title = "About Us • Manikrao Patil Education Foundation (MPEF)";
    } else if (isNews) {
      document.title = "News • Manikrao Patil Education Foundation (MPEF)";
    } else if (isGallery) {
      document.title = "Gallery • Manikrao Patil Education Foundation (MPEF)";
    } else if (isPrivacy) {
      document.title = "Privacy Policy • Manikrao Patil Education Foundation (MPEF)";
    } else if (isTerms) {
      document.title = "Terms of Service • Manikrao Patil Education Foundation (MPEF)";
    } else {
      document.title = "Manikrao Patil Education Foundation (MPEF)";
    }
  }

  function buildDomTranslationMaps() {
    document.querySelectorAll("[data-i18n]").forEach((el) => {
      const en = normalizeI18n(el.getAttribute("data-en") || "");
      const mr = normalizeI18n(el.getAttribute("data-mr") || "");
      if (en && mr) domMrByEn[en] = mr;
    });
    document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
      const en = normalizeI18n(el.getAttribute("data-en-placeholder") || "");
      const mr = normalizeI18n(el.getAttribute("data-mr-placeholder") || "");
      if (en && mr) domMrPlaceholderByEn[en] = mr;
    });
    document.querySelectorAll("[data-i18n-aria-label]").forEach((el) => {
      const en = normalizeI18n(el.getAttribute("data-en-aria-label") || "");
      const mr = normalizeI18n(el.getAttribute("data-mr-aria-label") || "");
      if (en && mr) domMrAriaByEn[en] = mr;
    });
  }

  function collectAutoTextNodes() {
    const scope = document.querySelectorAll("main *, footer *, header *");
    scope.forEach((el) => {
      if (!(el instanceof Element)) return;
      if (el.closest("[data-i18n]")) return;
      el.childNodes.forEach((node) => {
        if (node.nodeType !== Node.TEXT_NODE) return;
        const text = node.nodeValue || "";
        const trimmed = text.trim();
        if (!trimmed) return;
        autoTextNodes.push(node);
        autoTextOriginal.set(node, text);
      });
    });
  }

  function applyAutoTextTranslations(lang) {
    autoTextNodes.forEach((node) => {
      const original = autoTextOriginal.get(node);
      if (!original) return;
      const trimmed = original.trim();
      if (!trimmed) return;
      const mr = MR_BY_EN[trimmed];
      if (lang === "mr" && mr) {
        node.nodeValue = original.replace(trimmed, mr);
      } else {
        node.nodeValue = original;
      }
    });
  }

  // Lightbox
  const lightbox = document.querySelector("[data-lightbox]");
  const lbImg = document.querySelector("[data-lightbox-img]");
  const lbCaption = document.querySelector("[data-lightbox-caption]");
  const lbClose = document.querySelector("[data-lightbox-close]");
  let lbItems = [];
  let lbIndex = -1;

  let lbPrev = null;
  let lbNext = null;
  if (lightbox) {
    lbPrev = lightbox.querySelector("[data-lightbox-prev]");
    lbNext = lightbox.querySelector("[data-lightbox-next]");

    if (!lbPrev) {
      lbPrev = document.createElement("button");
      lbPrev.type = "button";
      lbPrev.className = "lightbox-nav lightbox-prev";
      lbPrev.setAttribute("data-lightbox-prev", "");
      lbPrev.setAttribute("aria-label", "Previous image");
      lbPrev.textContent = "<";
      lightbox.appendChild(lbPrev);
    }
    if (!lbNext) {
      lbNext = document.createElement("button");
      lbNext.type = "button";
      lbNext.className = "lightbox-nav lightbox-next";
      lbNext.setAttribute("data-lightbox-next", "");
      lbNext.setAttribute("aria-label", "Next image");
      lbNext.textContent = ">";
      lightbox.appendChild(lbNext);
    }
  }

  function collectLightboxItems() {
    return Array.from(document.querySelectorAll("[data-open-lightbox]")).filter(
      (el) => el instanceof HTMLElement
    );
  }

  function openLightbox(src, title, index = -1) {
    if (!lightbox || !(lbImg instanceof HTMLImageElement) || !lbCaption) return;
    lbImg.src = src;
    lbCaption.textContent = title || "";
    lbIndex = index;
    lightbox.classList.add("is-open");
    lightbox.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  }

  function closeLightbox() {
    if (!lightbox || !(lbImg instanceof HTMLImageElement) || !lbCaption) return;
    lightbox.classList.remove("is-open");
    lightbox.setAttribute("aria-hidden", "true");
    document.body.style.overflow = "";
    lbImg.src = "";
    lbCaption.textContent = "";
    lbIndex = -1;
  }

  function stepLightbox(direction) {
    if (!lbItems.length || lbIndex < 0) return;
    const total = lbItems.length;
    const next = (lbIndex + direction + total) % total;
    const item = lbItems[next];
    if (!(item instanceof HTMLElement)) return;
    const src = item.getAttribute("data-src") || "";
    const title = item.getAttribute("data-title") || "";
    openLightbox(src, title, next);
  }

  document.addEventListener("click", (e) => {
    const t = e.target;
    if (!(t instanceof Element)) return;
    const opener = t.closest("[data-open-lightbox]");
    if (opener instanceof HTMLElement) {
      lbItems = collectLightboxItems();
      const index = lbItems.indexOf(opener);
      const src = opener.getAttribute("data-src") || "";
      let title = opener.getAttribute("data-title") || "";
      const isMr = document.documentElement.getAttribute("data-lang") === "mr";
      if (isMr && MR_BY_EN[title]) title = MR_BY_EN[title];
      openLightbox(src, title, index);
    }
  });

  if (lbClose) lbClose.addEventListener("click", closeLightbox);
  if (lbPrev) lbPrev.addEventListener("click", () => stepLightbox(-1));
  if (lbNext) lbNext.addEventListener("click", () => stepLightbox(1));
  if (lightbox) {
    lightbox.addEventListener("click", (e) => {
      const t = e.target;
      if (!(t instanceof Element)) return;
      // click outside the image closes
      if (t === lightbox) closeLightbox();
    });
  }

  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") closeLightbox();
    if (!lightbox || !lightbox.classList.contains("is-open")) return;
    if (e.key === "ArrowLeft") stepLightbox(-1);
    if (e.key === "ArrowRight") stepLightbox(1);
  });

  function openEnquiryPopup(title, message, okText) {
    const old = document.querySelector(".enquiry-popup");
    if (old) old.remove();

    const popup = document.createElement("div");
    popup.className = "enquiry-popup";
    popup.innerHTML = `
      <div class="enquiry-popup-card" role="dialog" aria-modal="true" aria-label="${escapeHtml(title)}">
        <h3 class="enquiry-popup-title">${escapeHtml(title)}</h3>
        <p class="enquiry-popup-text">${escapeHtml(message)}</p>
        <button type="button" class="btn btn-primary enquiry-popup-btn">${escapeHtml(okText)}</button>
      </div>
    `;
    document.body.appendChild(popup);
    requestAnimationFrame(() => popup.classList.add("is-open"));

    const close = () => {
      popup.classList.remove("is-open");
      setTimeout(() => popup.remove(), 180);
    };

    const btn = popup.querySelector(".enquiry-popup-btn");
    if (btn) btn.addEventListener("click", close);
    popup.addEventListener("click", (e) => {
      if (e.target === popup) close();
    });
  }

  function escapeHtml(s) {
    return String(s)
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll('"', "&quot;")
      .replaceAll("'", "&#039;");
  }
})();


